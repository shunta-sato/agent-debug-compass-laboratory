# Workflow Contract Review

## Scope

- PR / branch: `codex/v024-pr6-suitability-linkage`
- Workflow surfaces:
  - `adc-lab decide suitability`
  - existing `workflow.collect_plan` pressure/composite steps from PR5
- Generated / consumed artifacts:
  - `pressure` artifacts
  - `report.operating_contract`
  - `report.suitability`
  - downstream `report.constraints`

## Source-of-truth Chain

| Stage | Artifact / command | Producer | Consumer | Notes |
| --- | --- | --- | --- | --- |
| plan | `workflow.collect_plan` | `collect plan` | Agent/operator execution | PR5 added pressure/composite argv for `target-characterization-full`. |
| pressure | `pressure/<kind>.<result_id>.v2.json` | generated `pressure run` argv | `decide suitability` | PR6 consumes typed `Artifact<PressurePayload>` payload semantics, not filename order. |
| contract | `report.operating_contract` | `report operating-contract` | `decide suitability` | Existing v2 contract remains part of the suitability evidence refs. |
| suitability | `report.suitability` | `decide suitability` | `constraints generate` | Storage/network/latency decisions now cite matching pressure refs where sufficient. |
| constraints | `report.constraints` | `constraints generate` | Agent/operator handoff | Unknown required dimensions still block `selection_ready`. |

## Producer / Consumer Consistency

| Producer | Artifact | Consumer | Required identity / semantic match | Result |
| --- | --- | --- | --- | --- |
| `pressure run --kind storage_io` | typed v2 `pressure` with `payload.pressure_kind=storage_io` and matching `target_id` | `decide suitability` storage dimension | measured-ish status plus effect observed; remains marginal because tempfile/page-cache smoke is not storage-device evidence | pass |
| `pressure run --kind network_io` without endpoint | typed v2 `pressure` with `network_mode=counter_only` / `Status::NotApplicable` and matching `target_id` | `decide suitability` network dimension | counter-only cannot support network meet and keeps required network unknown while preserving the concrete ref | pass |
| `pressure run --kind network_io --network-endpoint ...` | typed v2 `pressure` with `network_mode=bounded_transfer`, endpoint available, generated bytes, and matching `target_id` | `decide suitability` network dimension | endpoint-backed bounded transfer can become non-unknown, but remains marginal without throughput/loss/retry thresholds | pass |
| `pressure run --kind latency_jitter` | typed v2 `pressure` with measured-ish current-condition jitter and matching `target_id` | `decide suitability` latency dimension | current-condition jitter can become non-unknown, but remains marginal without workload latency SLO | pass |

## Generated argv Replay

PR6 does not add generated argv. It relies on PR5 collect-plan steps and checks
the typed v2 artifact envelope and payload fields written by those commands.
Suitability rejects pressure-like JSON, non-v2 pressure-looking JSON, unknown
pressure kinds, and pressure artifacts whose envelope `target_id` differs from
the workload target. Suitability does not infer storage/network/latency from
directory co-presence, filename ordering, mtime, or `latest` paths.

## Claim Boundaries

- `workflow.recommendation` and `workflow.collect_plan` remain workflow authority
  / handoff artifacts, not target measurement evidence.
- Counter-only network evidence remains unknown for required network
  suitability.
- Endpoint-backed bounded transfer is enough to avoid `unknown`, but not enough
  to claim network `meet` without policy thresholds.
- Tempfile/page-cache storage smoke is enough to avoid `unknown`, but not enough
  to claim storage-device `meet`.
- Current-condition latency jitter is enough to avoid `unknown`, but not enough
  to claim latency `meet` without workload latency thresholds.
- Unknown required dimensions continue to block `selection_ready`.

## Forbidden Fallback Checks

- No filename-order selection.
- No mtime/latest/newest artifact inference.
- No pressure-like JSON accepted without `schema=lab.artifact.v2`,
  `kind=pressure`, valid v2 `Status`, known pressure kind, and matching
  `target_id`.
- No network meet from rx/tx counters or counter-only evidence.
- No storage-device meet from tempfile/page-cache-only smoke.
- No selection readiness when a required dimension remains unknown.

## Findings

| ID | Severity | Finding | Required fix |
| --- | --- | --- | --- |
| none | n/a | PR6 links suitability dimensions only to typed v2 pressure artifact payload semantics with matching target identity and preserves conservative claim boundaries. | n/a |

## Decision

submit
