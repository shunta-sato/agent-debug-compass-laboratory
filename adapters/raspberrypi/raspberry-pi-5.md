# Raspberry Pi 5 Adapter Notes

Initial controller class for `adc-lab`.

Expected role:

- Build and run `adc-lab`.
- Orchestrate SSH-based fixed target commands.
- Store run artifacts and reports.

MVP limitation:

- Host fallback tests on this development environment do not prove Pi5 resource behavior.
